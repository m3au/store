[core]
    autocrlf = false
    safecrlf = true
    ignorecase = false

[lfs]
    enabled = true

[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
